<%
=begin
  Creates .netrc for use with GitHub over HTTP

  This is necessary to get around draconian corporate proxies.
=end
%>
machine github.com
        login <%= print("GitHub Username: "); STDOUT.flush; STDIN.gets.chomp %>
        password <%= print("GitHub Password: "); STDOUT.flush; STDIN.gets.chomp %>
