" Local customizations overriding vim/gvimrc settings

" Transparency (must have experimental renderer enabled within MacVim)
set transparency=5

" Default color scheme
colorscheme neon

" Code width visual guide
highlight ColorColumn guibg=#404040
set colorcolumn=80

" Fonts
set guifont=Inconsolata:h16
set antialias
