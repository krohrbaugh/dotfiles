# Sublime Text 3 settings

Most settings are automatically configured when running `script/setup`, but
it's still necessary to manually install Package Control in Sublime.

Refer to the directions on https://packagecontrol.io/installation for
updated directions.