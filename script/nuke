#!/bin/bash
#
# nuke dotfiles install with extreme prejudice (no confirmation)

DOTFILES_ROOT="`pwd`"
source "${DOTFILES_ROOT}/script/shared/common.sh"
source "${DOTFILES_ROOT}/script/shared/uninstall.sh"

set -e

echo ''
delete_all
echo ''
echo '  Nuked!'
